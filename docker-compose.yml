version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:7860"
    env_file:
      - .env
    environment:
      - ENV=development
    volumes:
      - ./app:/app/app   # hot-reload en dev
    restart: unless-stopped
